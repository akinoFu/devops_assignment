pipeline {
    agent any
    parameters {
        booleanParam(defaultValue: false, description: 'whether to Run the Code', name: 'RUN')
        text(defaultValue: 'Development', description: 'Type of build', name: 'BUILD_TYPE')
    } 
    stages {
        stage('Build') {
            steps { 
                sh "pip install -r requirements.txt"
            }
        }
        // stage('Package') { 
        //     when { 
        //         expression { env.GIT_BRANCH == 'main' } 
        //     } 
        //     steps { 
        //         withCredentials([string(credentialsId: 'akino_dockerhub', variable: 'TOKEN')]) { 
        //             sh "docker login -u 'akinofu' -p '$TOKEN' docker.io" 
        //             sh "docker build --tag akinofu/${dockerRepoName}:latest ${directoryName}" 
        //             sh "docker push akinofu/${dockerRepoName}:latest" 
        //         } 
        //     } 
        // }
}
}